# GC2025
Pothole Detection System using computer vision and machine learning. 

Using a Raspberry pi running the edge impulse software we can run a object detection model so that the input from a camera module can be output to a screen.